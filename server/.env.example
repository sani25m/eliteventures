PORT=3001
ADMIN_PASSWORD=change-me-in-production
ADMIN_TOKEN=your-secret-admin-token-change-in-production
FRONTEND_ORIGIN=http://localhost:5173

# Supabase (optional – if set, submissions are stored in Supabase instead of local JSON file)
# Get these from Supabase Dashboard → Project Settings → API
# SUPABASE_URL=https://your-project-ref.supabase.co
# SUPABASE_ANON_KEY=your-anon-key (required for client auth / my-submissions)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
